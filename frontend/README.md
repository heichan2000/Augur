# Augur — Frontend

Next.js + TypeScript + Tailwind CSS frontend for Augur, the developer-docs AI assistant powered by Claude.

## Local development

See the root README for full setup instructions (including the backend).

```bash
npm install
npm run dev
```

The app runs at [http://localhost:3000](http://localhost:3000).
